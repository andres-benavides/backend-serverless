{
  "name": "amm-purchase-approvals",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "check:backend": "npm --prefix backend run lint && npm --prefix backend run format:check && npm --prefix backend run typecheck",
    "check:frontend": "npm --prefix frontend run lint && npm --prefix frontend run format:check && npm --prefix frontend run typecheck",
    "check": "npm run check:backend && npm run check:frontend",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend test",
    "test": "npm run test:backend && npm run test:frontend",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build",
    "build": "npm run build:backend && npm run build:frontend"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
